/**
 * Not gate:
 * if (in) out = 0, else out = 1
 */
CHIP Not {
    IN in;
    OUT out;

    PARTS:
    Nand(a=in, b=in, out=out);
    // single input value for NOT
    // pumped into both inputs of a NAND
    // inverts the input
}